---
import BaseLayout from '../../layouts/BaseLayout.astro'
import { getCollection } from 'astro:content'

const posts = (await getCollection('blog', ({ data }) => !data.draft))
  .sort((a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime())
---

<BaseLayout
  title="Articles — RIIF"
  description="Research articles, technical documentation, and project updates from RIIF."
>
  <nav class="border-b border-slate-100 px-4 py-3 sticky top-0 z-40 bg-white/95 backdrop-blur-sm">
    <div class="max-w-3xl mx-auto flex items-center justify-between">
      <a href="/" class="text-lg font-bold text-slate-800">RIIF</a>
      <div class="flex items-center gap-4 text-sm">
        <a href="/blog" class="text-teal-600 font-medium">Articles</a>
        <a href="https://riif.com" class="text-slate-500 hover:text-slate-800">riif.com</a>
      </div>
    </div>
  </nav>

  <main class="max-w-3xl mx-auto px-4 py-12">
    <h1 class="text-3xl font-bold text-slate-800 mb-2">Articles</h1>
    <p class="text-slate-500 mb-10">Research, documentation, and technical writing.</p>

    <div class="space-y-8">
      {posts.map((post) => (
        <a href={`/blog/${post.id.replace(/\.md$/, '')}`} class="block group">
          <article class="border border-slate-200 rounded-xl p-6 hover:border-teal-200 hover:shadow-sm transition-all">
            <div class="flex flex-wrap items-center gap-2 mb-3">
              {post.data.tags.map((tag) => (
                <span class="text-xs font-medium text-teal-700 bg-teal-50 px-2 py-0.5 rounded-full">{tag}</span>
              ))}
            </div>
            <h2 class="text-xl font-bold text-slate-800 group-hover:text-teal-600 transition-colors mb-2">{post.data.title}</h2>
            <p class="text-slate-500 text-sm mb-3">{post.data.description}</p>
            <div class="flex items-center gap-3 text-xs text-slate-400">
              <span>{post.data.author}</span>
              <span>&middot;</span>
              <time datetime={post.data.date}>
                {new Date(post.data.date).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })}
              </time>
            </div>
          </article>
        </a>
      ))}
    </div>
  </main>

  <footer class="border-t border-slate-100 px-4 py-8">
    <div class="max-w-3xl mx-auto text-center">
      <div class="flex justify-center gap-4 text-xs text-slate-400 mb-3">
        <a href="https://haven.riif.com" class="hover:text-slate-600">Haven Phone</a>
        <a href="https://paragondao.org" class="hover:text-slate-600">ParagonDAO</a>
        <a href="https://riif.com" class="hover:text-slate-600">RIIF</a>
      </div>
      <p class="text-xs text-slate-400">Univault Technologies LLC &middot; Salt Lake City, Utah</p>
    </div>
  </footer>
</BaseLayout>
